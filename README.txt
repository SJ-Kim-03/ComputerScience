Road to Developer.

git init
- .git 폴더: local repository 생성
- 초기화 과정
- shift + command + . : 숨김 파일 확인

git config -- global user.email(.name) "email(name)"
- 수정자 정보 등록
- 이메일, 이름 입력

git add file
- 버전으로 만들 파일 선택

git commit -m
- 커밋의 상세 설명 추가

git log
- 커밋 확인 가능, 최신순

git checkout